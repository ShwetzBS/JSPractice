"use strict";

function calcAge(birthYear) {
  const age = 2024 - birthYear;
  console.log(firstName);
  return age;
}

console.log(calcAge(1994)); // will give error as firname not defined
const firstName = "Shweta";

// const firstName = "Shweta";
// console.log(calcAge(1994)); // will work as firstname got defined first
